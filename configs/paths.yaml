project_root: .

# Centralized filesystem roots.
# All scripts should resolve paths relative to the repository root.
data_root: data
outputs_root: outputs

# Dataset-specific roots (no absolute paths).
datasets:
  EFNY:
    data_root: data/EFNY
    outputs_root: outputs/EFNY
