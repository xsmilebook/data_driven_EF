# 2026-04-14

## 本次会话

- 根据当前预处理需求，重写 `ARCHITECTURE.md` 中的 `src/` 与 `scripts/` 结构说明。
- 将影像流程明确拆分为 `fmriprep`、`xcpd` 与 `connectivity` 三个子目录。
- 将行为流程明确拆分为 `app`、`task_fmri`、`inventory`、`demography` 四类数据域，并为主要行为任务采用 `check_format -> clean -> metrics/score` 的三阶段组织。
- 更新 `README.md` 中的核心目录概览。
- 在 `docs/workflow.md` 中补充预处理脚本组织原则。
