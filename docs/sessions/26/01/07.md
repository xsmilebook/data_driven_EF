# 2026-01-07 会话记录

## 变更目的
检查 `data/raw/MRI_data/task_psych/` 下是否存在“同一被试同一任务对应多个 Psychopy CSV 记录”的情况，并输出可复核的汇总表，便于后续决定筛选策略（例如取最新一次、或按时间戳匹配扫描序列）。

## 修改内容
- 新增重复文件扫描脚本：`temp/find_repeated_task_psych_csvs.py`。
- 生成汇总报告：`data/interim/MRI_data/repeated_task_psych_file/repeated_task_psych_files.tsv`（列出每个 subject×task 的重复 CSV 列表，并给出最新文件）。
- 将 task-fMRI confounds 构建中的“行为 CSV 选择策略”改为按 **文件名时间戳**（`YYYY-MM-DD_HHhMM.SS.mmm`）选择最新记录（不再依赖文件修改时间）。
