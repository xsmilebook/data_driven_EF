# 会话记录（2026-01-03）

目标读者：研究协作成员与复现审阅者。

范围：建立 DDM/HDDM（PyMC-based）决策文档生成与层级 HDDM（回归）计算的最小可复现流水线。

变更：
- 新增 `scripts/ddm_decision_report.py`：从 `data/raw/behavior_data/cibr_app_data/` 读取 app 行为 Excel，抽取冲突与切换任务的试次级数据，执行层级 HDDM（HSSM + PyMC，`nuts_numpyro`），并将结果表格嵌入 `docs/reports/ddm_decision.md`。
- 新增 `src/behavioral_preprocess/ddm/`：封装试次抽取与 HSSM 拟合逻辑，避免在脚本中散落任务口径。
- 在 `docs/workflow.md` 增补 DDM/HDDM 决策与建模入口说明。

复现：

```bash
python -m scripts.ddm_decision_report --dataset EFNY --config configs/paths.yaml
```

备注：
- 当前环境未检测到 `g++`，因此采用 `nuts_numpyro` 以降低 PyTensor 编译依赖。
- 建议先用 `--max-files` 进行 pilot，再扩展到全样本；本仓库默认仅将结果嵌入 Markdown 报告，不额外写 CSV。

