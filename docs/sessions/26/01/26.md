# 会话记录（2026-01-26）

目标读者：贡献者与可复现性审阅者。

范围：对齐 APP 更正结果与原始去重清单，移除重复被试输出并记录说明。非目标：不改动原始工作簿或更正逻辑。

变更：
- 在 `data/processed/behavior_data/cibr_app_data_corrected_excel/run_corrected_v1/` 中移除 8 个重复被试的更正输出（含 `subjects/` 与 `decisions/` 以及 `manifest.*` 条目）。
- 在 `docs/reports/app_sequence_correction_report.md` 补充与 `_excluded_duplicates` 对齐的手动剔除记录说明。

备注：
- 保留 `THU_20250725_684_LC_李晨_GameData.xlsx`，仅删除同日重复的 `THU_20250725_684_LC_刘元博_GameData.xlsx`。

补充：
- 复核 2025-12-07/12-13 低分被试的答案序列匹配情况，整理出非 visit2 的证据并记录到 `docs/notes/app_data_problem.md`。
- 汇总 visit1–visit4 序列改动与差异清单至 `docs/reports/visit_sequence_changes.md`。
- 检查 20231014–20250707 被试与 visit1 的一致性，并输出任务/被试级差异清单至 `docs/reports/visit1_consistency_20231014_20250707.md`。
- 审计 `data/interim/behavioral_preprocess/` 下的序列分组产物并输出 `docs/reports/app_grouping_audit.md`；草拟按“日期顺序 + 序列类型”逐个清洗的 v2 流程至 `docs/reports/app_data_cleaning_v2_workflow.md`。
