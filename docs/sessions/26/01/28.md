# 2026-01-28

## 本次目标

- 按 item 分组（group_001–group_004）在每组内选择**完成 18 个任务（18 个 sheet）**的代表被试进行 item 序列对比，并将 KT/ZYST/SST 纳入对比。

## 已完成工作

- 更新 `docs/reports/item_group_sequence_changes.md`：
  - 代表被试选择规则改为“每组优先选 18-sheet、若多名则取日期最早”。
  - 在对比口径中纳入 `KT`、`ZYST`、`SST`，并补充 `SST` item 近似空序列、`KT` item 记录稀疏导致 `match_ratio` 易被放大的注意事项。
  - 补充原始工作簿命名与 `subject_id` 的前缀匹配规则（`{subject_id}_*_GameData.xlsx`）。
- 规则更正：将 `KT` 与 `FZSS` 明确纳入 item 侧证据（两者均存在 item 序列变动/修改记录）；相应更新 v2 清洗文档中的“任务特例”表述。

## 产出/变更文件

- `docs/reports/item_group_sequence_changes.md`
- `docs/reports/app_data_cleaning_v2_workflow.md`
- `docs/reports/app_sequence_correction_report.md`
- `docs/reports/visit_sequence_changes.md`
- `PROGRESS.md`
