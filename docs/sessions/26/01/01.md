# 会话记录（2026-01-01）

目标读者：研究协作成员与复现审阅者。

范围：更新 PLAN.md 以匹配科研项目的计划模板；不涉及代码与数据。

变更：
- 将 PLAN.md 从通用工程模板调整为科研计划模板，补充研究目标、数据与样本、方法与模型、验证设计、评价指标、阶段计划与风险约束等要素。

备注：
- 未变更目录结构、数据路径与运行流程。

补充变更：
- 在 AGENTS.md 中新增约束：根目录 Markdown 文档仅允许使用英文。
- 在 AGENTS.md 中新增规则：若使用 create-plan skill，计划需写入根目录 PLAN.md。
- 在 methods.md 中补充各指标计算流程与口径说明。
- 在 behavioral_metrics.yaml 中移除 SST/GNG/CPT 的 RT_Mean/RT_SD 输出，并同步更新 methods.md。

补充变更（行为数据 EDA）：
- 在 `scripts/eda_behavior_trials.py` 增加单被试工作簿的探索性分析入口：按指标计算口径汇总各任务总试次与条件试次（Go/NoGo、Repeat/Switch、Congruent/Incongruent、N-back Match/NoMatch），并生成 DDM 适用性建议段落。
- 在 `notebooks/eda_behavior_trials.py` 提供与脚本入口一致的便捷运行封装。
- 在 `configs/paths.yaml` 补充 `docs_reports_root` 与 `dataset.behavioral.reference_game_data_file`，使输入/输出路径均可由配置集中管理。
- 生成报告：`docs/reports/behavior_data_eda_thu_20250829_759_jzc.md`（对应 `THU_20250829_759_JZC_景志成_GameData.xlsx`）。

更新（SST 试次统计）：
- 在 EDA 统计中为 SST 增加 Go/Stop 试次计数（按 `SSD/SSRT` 列是否为空判定 stop），并同步更新报告内容。
