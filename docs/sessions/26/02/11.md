# 2026-02-11

## 本次目标

- 按用户要求将 `AGENTS.md` 全文改为中文，并补充 Windows 环境下使用 `uv` 管理依赖的规则。

## 已完成工作

- 将 `AGENTS.md` 全文翻译为中文，保留原有结构与执行约束。
- 在“工程规范”新增规则：
- Windows 环境统一使用 `uv` 管理依赖。
- 新增依赖使用 `uv add <package>`。
- 项目虚拟环境固定为仓库根目录 `.venv`。
- 同步更新 `PROGRESS.md`。
- 按用户要求先单独提交已有依赖变更（`pyproject.toml`、`uv.lock`，新增 `openpyxl`）。
- 新增脚本 `scripts/build_visit3_library_v2.py`，基于 `item_group_002 + answer_group_002` 在 `run_corrected_v2/sequence_library` 生成 visit3 候选序列，并与 `app_sequence/visit3` 自动比对。
- 新增报告 `docs/reports/visit3_group2_export_comparison.md`，给出“导出 visit3 与 `app_sequence/visit3` 不匹配”的结论与任务级摘要。

## 变更文件

- `AGENTS.md`
- `PROGRESS.md`
- `pyproject.toml`
- `uv.lock`
- `scripts/build_visit3_library_v2.py`
- `docs/reports/visit3_group2_export_comparison.md`
- `docs/README.md`
