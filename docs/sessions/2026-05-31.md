# 2026-05-31

## 本次会话

- 实现 THU `app_data` 的三阶段正式预处理入口：`check_format -> clean -> metrics`。
- 新增路径配置与行为指标配置，统一维护中文字段映射、RT 闭区间 `[0.2, 10]`、
  18 类任务注册信息和随机正确率阈值。
- 新增 app 行为数据格式检查、试次清洗、任务 QC、指标长表和指标宽表生成逻辑。
- 按任务家族实现 N-back、冲突任务、任务切换、SST、GNG/CPT、ZYST、FZSS、KT 指标。
- 初版 `dprime` 未做有限样本校正；该口径已在本次会话后续替换为 Hautus
  log-linear 校正。
- 初版 EmotionStroop 条件映射保留显式配置接口；已在本次会话后续写入确认后的编码。
- 使用 3 个 THU workbook 做非持久化冒烟测试：覆盖 18 类任务、4183 行清洗试次、
  352 行指标和 3 行被试级宽表；识别出 266 行 RT 无效试次，并在验证后清理临时结果。
- 执行扩展断言，验证 RT 闭区间筛选、随机正确率阈值、任务规则抽样、
  EmotionStroop 空映射处理、dprime 边界空值和无效任务指标 masking。

## THU app 指标口径修复

- 将准确率有效试次与 RT 有效试次分离：RT 为空仅排除 RT 指标；RT 非空但非数值
  或超出 `[0.2, 10]` 秒范围时同时排除准确率和 RT 指标。
- 任务 QC 新增 `n_trials_acc_valid`，并保留真实的 `n_trials_rt_valid`。
- CPT/GNG 继续使用原始 `answer` 布尔值分类，恢复 CPT 无 RT nogo trial 对
  `NoGo_ACC` 与 `dprime` 的贡献。
- 写入已确认的 EmotionStroop `e1-e32` 条件映射。
- Emotion N-back 在 target 分类前先归一化情绪类别；Emotion2Back item 缺失时
  保留整体指标并记录 `nback_item_missing`。
- KT 简化为仅输出 `ACC`。
- workbook 枚举忽略 Excel 产生的 `~$*.xlsx` 临时锁文件，避免全量任务误读临时文件。
- 使用 3 个 workbook 做冒烟验证：覆盖 18 类任务、4183 行清洗试次和 340 行指标；
  其中 247 行 `rt_missing` trial 保留在准确率分母。CPT 的 `NoGo_ACC`、`dprime`
  与 EmotionStroop 条件指标均已恢复有效输出。
- 重新生成 753 个 workbook 的全量 `app_trials_clean.csv`、`app_task_qc.csv`、
  `app_metrics_long.csv` 和 `app_metrics_wide.csv`：`app_clean` 用时 10 分 46 秒，
  `app_metrics` 用时 38 秒。
- 全量结果包含 1,104,613 行 trial、13,097 行任务 QC 和 90,204 行长格式指标。
  CPT 保留 61,824 个 nogo trial，其中 57,420 个 RT 为空；`CPT_NoGo_ACC`
  均值为 0.937，`CPT_dprime` 有 323 个非空值。
- EmotionStroop 的一致、不一致和 contrast 指标均有 726 个非空值；Emotion N-back
  类别归一化规则与原始答案在 81,345 个可比较 trial 上完全一致。除按既定边界规则
  置空的 CPT `dprime` 外，未发现全空指标、常数指标或高缺失指标。

## dprime 边界比例校正

- 将 N-back 与 GNG/CPT 的 `dprime` 改为 Hautus log-linear 校正：命中数和虚报数
  分别加 `0.5`，对应分母加 `1`。
- 原始 `Hit_Rate`、`FA_Rate`、`Go_ACC` 与 `NoGo_ACC` 继续按未校正比例输出。
- 命中率或虚报率观察值为 `0` 或 `1` 时保留有限 `dprime`；仅在 signal 或 noise
  trial 数为 `0` 时输出空值。
- 清洗长表未变化，因此直接重跑全量 `app_metrics`，用时 40 秒。`CPT_dprime`
  非空数由 323 增加到 726，覆盖全部通过任务 QC 的 CPT sheet；未发现无穷值、
  全空指标或高缺失指标。

## THU app 指标 EDA 与可视化报告

- 新增 ignored notebook `notebooks/app_data_eda/app_metrics_eda_report.ipynb`，直接读取
  正式处理后的 trial、QC、长表和宽表 CSV，不重新扫描原始 Excel。
- 使用 `uv add matplotlib seaborn` 增加静态科研图依赖。
- notebook 分块扫描 1,104,613 行 trial，导出任务覆盖/QC、trial 排除、指标缺失、
  描述统计、IQR 异常值候选、Pearson、Spearman 和 pairwise N 表。
- 生成 9 张 PNG 和隐藏代码输入的聚合 HTML：
  `outputs/results/app_data_eda/app_metrics_eda_report.html`。
- 全量 notebook 自顶向下执行通过。验证 753 名被试、18 类任务、124 个宽表指标；
  未发现无穷值、常数指标列或超出 `[0, 1]` 的比例指标。
- HTML 隐私检查通过：未包含实际 `subject_code` 值、姓名或原始账号。个体级内部 QC
  CSV 仅保留 `subject_code`，用于定位复核对象。
