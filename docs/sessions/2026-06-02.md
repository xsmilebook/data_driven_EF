# 2026-06-02

## 高频低准确率任务分析与 Emotion2Back 指标保留

- 核查 `acc_below_random_threshold` 超过 100 行的任务：
  `Emotion2Back`（163/708）、`Spatial2Back`（118/714）、GNG（111/737）和
  `Emotion1Back`（103/725）。
- 三类 N-back 低分组均表现出更高缺按键率和更低的已按键 trial 正确率，说明低分
  同时包含漏答和错误反应，不是 RT 范围清洗造成。
- GNG 低分组与非低分组的答案构成均约为 80% Go、20% NoGo。低分组按 Go 键比例
  中位数仅为 24.0%，Go 正确率为 25.5%，NoGo 正确率仍为 82.0%，更接近 Go
  反应不足。
- Emotion2Back 中 43 个 `nback_item_missing` sheet 的 item 均为全空，主要聚集于
  2025-07。修改指标输出掩码：即使 item 全空且 ACC 低于随机阈值，也在长表和宽表
  中保留可直接由 `answer == key` 计算的 ACC；任务 QC 继续标记失败，依赖 item 的
  `Hit_Rate`、`FA_Rate` 和 `dprime` 保持为空。
- 使用内联断言验证选择性掩码分支后，重跑全量 `app_metrics`、EDA notebook 和
  HTML 导出。Emotion2Back ACC 非缺失数由 544 增加到 552，其中新增的 8 行对应
  item 全空且低于随机阈值的 sheet。
- EDA 内置断言、HTML 编码和隐私检查通过；未发现连续问号乱码、Unicode 替换字符
  或个体标识泄露。
