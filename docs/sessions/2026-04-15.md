# 2026-04-15

## 本次会话

- 在 `notebooks/app_data_eda/` 下新增 `app_data_trial_cleaning_parameter_exploration.ipynb`，用于探索 `app_data` 试次清洗参数。
- 基于 `data/raw/*/app_data/*.xlsx` 中全部原始 app 行为文件，统计 `相对时间(秒) < 0.1` 与 `< 0.2` 的无效试次情况。
- 在同一 notebook 中补充每个 sheet 内 `相对时间(秒) > mean + 3 s.d.` 的高阈值无效试次统计。
- 在同一 notebook 中补充 `threshold=0.1` 与 `threshold=0.2` 下无效试次平均正确率统计，正确判定规则为 `正式阶段正确答案 == 正式阶段被试按键`。
- 在 `docs/workflow.md` 中补充该探索性 notebook 的用途与统计口径说明。
