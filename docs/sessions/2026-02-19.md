# Session Log - 2026-02-19

## 用户目标

- 冻结当前版本（可回滚、可追溯）。
- 在不破坏现有结构前提下，提供基于已清洗数据的 v2 最小开发骨架。
- 给出可执行的迁移验收清单。

## 本次变更

- 新增 `scripts/archive_legacy_snapshot.py`：
  - 预演模式（默认）：只输出计划执行的 git 命令。
  - 执行模式（`--execute`）：创建 archive 分支与注释 tag。
  - 可选 `--push` 推送到远端；默认脏工作区拒绝执行（可 `--allow-dirty`）。
- 新增 `scripts/v2_run_pipeline.py` 与 `src/models/v2_pipeline.py`：
  - 解析 dataset/config。
  - 定位清洗后行为表（默认 `dataset.files.behavioral_metrics_file`）。
  - dry-run 输出运行计划；非 dry-run 输出 profile 元信息。
- 新增文档：
  - `docs/reports/v2_minimal_dev_skeleton.md`
  - `docs/reports/v2_migration_acceptance_checklist.md`
- 更新文档：
  - `README.md`
  - `docs/README.md`
  - `docs/workflow.md`
  - `PROGRESS.md`

## 验证

- 计划运行：
  - `python -m scripts.archive_legacy_snapshot --archive-name v1_freeze_20260219`
  - `python -m scripts.v2_run_pipeline --dataset EFNY --config configs/paths.yaml --dry-run`

