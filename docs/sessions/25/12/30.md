# 会话记录（2025-12-30）

目标读者：贡献者与可复现性审阅者。

范围：文档、目录布局与提交脚本中的日志路径更新。非目标：不变更流水线或分析逻辑。

变更：
- 新增 `notebooks/` 与 `models/` 根目录，用于探索性工作与模型产物。
- 将 `docs/report/` 移动为 `docs/reports/`，并更新相关引用。
- 将运行日志迁移至 `outputs/logs/`（移除顶层 `logs/`，更新引用与脚本）。
- 在 `PROJECT_STRUCTURE.md` 中补充 `data/external/` 说明。
- 更新 SLURM 提交脚本，将日志写入 `outputs/logs/`。
- 在 `PROJECT_STRUCTURE.md` 中明确取消 `data/*/` 与 `models/` 的数据集层级。
- 将 `PLAN.md` 与 `PROGRESS.md` 重置为空结构模板。
- 扩展 `AGENTS.md`，补充基础工作流、范围规则与文档/工程约定。
- 在 `docs/methods.md` 中补充嵌套 CV 与置换检验的详细说明。
- 将 `docs/` 下文档统一为中文，并在 `AGENTS.md` 中注明文档语言与目录命名约定。
- 采用单一 EFNY 数据集路径方案，去除 `data/` 与 `outputs/` 的数据集层级。
- 合并 EFNY 数据集配置与说明：配置移入 `configs/paths.yaml`，文档并入 `docs/workflow.md`。
- 补充 `docs/data_dictionary.md` 中 QC 与 FC 相关文件说明。
- 在 `docs/workflow.md` 增加预处理流程章节，并在 `docs/methods.md` 补充方法学细节。
- 重新整理 `docs/workflow.md` 与 `docs/methods.md`，修正中文编码乱码。
- 补充影像与行为预处理的详细步骤（xcp-d、头动 QC 标准、行为清洗与映射）。
- 重构 `src/` 目录：拆分为 `src/imaging_preprocess/` 与 `src/behavioral_preprocess/`，并统一命名风格与引用路径。
- 将 `preprocess_efny_demo.py` 移入行为预处理模块，人口学默认归入行为数据流程。
- 删除基于 task 配置表的旧指标流程，仅保留 `DEFAULT_TASK_CONFIG`。

备注：
- 目录结构已在 `PROJECT_STRUCTURE.md`、`docs/workflow.md` 与研究文档中同步体现。
